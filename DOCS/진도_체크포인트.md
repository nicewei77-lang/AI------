# 진도 체크포인트 (살아있는 진행 기록)

> 에이전트는 **매 세션 시작 시 이 파일을 먼저 읽어** 어디서 이어갈지 파악하고,
> **각 단계/세션 끝에 이 파일을 갱신**한다. (커리큘럼 전체 지도는 학습코치 AGENT §5·§6, 회고는 학습_지침_개선_일지.md)

마지막 업데이트: 2026-06-10 (프론트 종료 → 백엔드 전환)

> **단계 기준 = `ALIBAI_프론트_학습_단계별_가이드.md`의 9단계 번호.** (괄호 안은 학습코치 AGENT의 슬라이스 표기) 둘이 어긋나면 가이드 순서를 따른다.

---

## 현재 위치

- **진행 중 단계:** 7단계까지 직접 완료. **8·9단계는 시간 관계로 코치가 임시 채움**(아래 "임시 채움 되돌리기" 참고) → 프론트 일단락, **백엔드로 전환.**
- **다음 할 일:** 백엔드 시작. 초안 커리큘럼 = `DOCS/ALIBAI_백엔드_학습코치_초안.md` (2.5일, 바텀업). 첫 세션은 B1(환경·설정·FastAPI 골격). 프론트 8·9단계는 "보충 시간"에 임시 채움을 지우고 직접 재작성.
- **막판 메모(세션7 후반=7-2):** 7단계 완료. `App.tsx`에 `<Route path="/posts/:id" element={<PostDetailPage/>}/>` 추가, `PostDetailPage.tsx` 신규 작성(`useParams()`로 `{id}`꺼냄→`fetchPostById(id!)`, 의존성 `[id]`, `Post|null` 초기 null + `if(!post) return null` 가드, loading/error 3갈래), `PostCard`의 제목을 `<h2><Link to={`/posts/${post.id}`} className="hover:underline">`로 감쌈. `tsc -b`·`lint` 통과 + `npm run dev`로 목록↔상세 왕복 동작 확인. 이번 세션 버그 5개(전부 7-2 개념노트에 정리): 라우트 콜론 누락(`/posts/id`→`:id`), 컴포넌트명 소문자(`postDetailPage`), `useState<T>` 괄호 누락(TS2488), `PostListPage` 함수 통째 중복(TS2393), `<Link>` 중괄호 누락+경로 단/복수 불일치(`/post`→`/posts`). 개념 Q&A: 클라이언트 사이드 라우팅 등장배경·History API(pushState/popstate)·state 보존 원리(메모리 리셋 vs 화면만 교체), `<h2>` 안 `<Link>` 이유(의미 vs 기능 분리). 코칭 메모: 세션6의 mockData 태그 일관성(t1~t4)·잔여 주석 정리 여전히 미적용.

---

## 단계별 상태

```
[x] 1단계  P0 확인 (Vite/Tailwind/Router, frontend 골격) — 기세팅 확인
[x] 2단계  TS 타입: Verdict/ExcuseContext/Post/Tag/NewPost (1-A) — tsc 통과
[x] 3단계  Day1 mock 데이터: api/mockData.ts MOCK_POSTS (게시글 3개 p1~p3) — tsc 통과
[x] 4단계  컴포넌트+props+map → PostCard/PostList (1-B) — tsc·lint 통과
[x] 5단계  Day1 페이지 조립: PostListPage + App.tsx 라우터 연결 — tsc·lint 통과
[x] 6단계  Day2 mock API: api/posts.ts (fetchPosts/fetchPostById/createPost) — tsc·lint 통과
[x] 7단계  Day2 useEffect + 상세페이지 (useParams, loading/error, Link/라우팅) (2-B) — tsc·lint·dev 확인
[T] 8단계  Day2 폼 드릴: useState+onChange+onSubmit → LoginPage/ExcuseForm (2-A)  — 코치 임시 채움(보충 때 직접 재작성)
[T] 9단계  Day3 Context+ProtectedRoute → /new 보호 (3-A) — 코치 임시 채움(보충 때 직접 재작성) [검색/태그·페이징=미구현]
```
(`x`=직접 완료, `~`=진행중, `T`=코치 임시 채움(되돌려 직접 작성 예정), ` `=미착수)

---

## ⚠️ 임시 채움 되돌리기 (보충 시간에 직접 작성할 파일)

> 8·9단계는 백엔드로 빨리 넘어가려고 **코치가 임시로 동작 코드를 채워 둔 것**이다.
> 각 파일 맨 위에 `// ⚠️ [임시 채움 — 코치] ...` 표식이 있다. 보충 시간엔 **그 파일들을 아래 "되돌릴 상태"로 지운 뒤 직접 드릴로 재작성**한다.
> (tsc·lint 통과 + `npm run dev`로 로그인→/new 보호→작성→상세 이동 동작 확인까지 해 둠.)

| 단계 | 파일 | 되돌릴 상태 | 다시 드릴할 개념 |
|---|---|---|---|
| 8 | `frontend/src/pages/LoginPage.tsx` | `export {}` | useState 제어폼·onSubmit·preventDefault·useNavigate |
| 8 | `frontend/src/components/ExcuseForm.tsx` | `export {}` | controlled form·부모 콜백(onSubmit props) |
| 9 | `frontend/src/api/auth.ts` | `export {}` | mock login 시그니처(계약형) |
| 9 | `frontend/src/context/AuthContext.tsx` | `export {}` | createContext/Provider/useContext 커스텀 훅 |
| 9 | `frontend/src/components/ProtectedRoute.tsx` | `export {}` | `<Navigate replace/>` 가드 |
| 9 | `frontend/src/pages/PostCreatePage.tsx` | `export {}` | ExcuseForm↔createPost 연결·작성 후 이동 |
| 8·9 | `frontend/src/App.tsx` | `/login`·`/new` 라우트 3줄과 그 import 제거 | ProtectedRoute로 라우트 감싸기 |
| 9 | `frontend/src/main.tsx` | `AuthProvider` 래핑과 import 제거 | Provider를 앱 최상단에 올리기 |

(미구현으로 남긴 것: 검색 input·태그 필터·페이징 UI — 9단계 잔여 기능. 직접 재작성 때 같이 한다.)

---

## 설계 결정 로그 (스펙과 달라진 점 — 다음 세션이 헷갈리지 않게)

- **2026-06-07:** `Post`에서 `situation` 제거, `title` 추가. 변명 유형 분류는 `tags`가 전담.
  (이유: `situation`과 `tags`가 둘 다 "지각/결석/마감" 분류라 역할 중복. 게시판이니 제목이 필요.)
  → 코드(`types/post.ts`), 기획서 DB 스키마·기능표, 학습코치 AGENT 드릴 예시 모두 반영 완료.
- **2026-06-08:** `fetchPosts`의 태그 필터 파라미터 이름을 `tag` → `tagId`로 변경(`api/posts.ts`).
  (이유: `tag`가 Tag 객체처럼 읽혀 혼란. 실제 담는 값은 id 문자열이라 `t.id === params.tagId`로 의도가 명확해짐.)
  → 다음 주 axios 전환 시 URL 쿼리 키는 `?tag=`이지만 함수 본문에서 `{params:{tag: tagId}}`로 변환하면 되므로 바깥 시그니처 `tagId` 유지 가능. (가이드/기획서 드릴 예시엔 아직 미반영 — 추후 동기화 대상.)

---

## 완성한 산출물

- `frontend/src/types/post.ts` — `Verdict`, `ExcuseContext`, `Post`, `Tag`, `NewPost`
- `frontend/src/components/PostCard.tsx` — `Post` 1개를 props로 받아 카드 렌더 (첫 `.tsx`)
- `frontend/src/components/PostList.tsx` — `Post[]`를 `.map()`으로 PostCard 목록 렌더 (`key={post.id}`)
- `frontend/src/pages/PostListPage.tsx` — `MOCK_POSTS`를 직접 import해 `<PostList posts={MOCK_POSTS}/>`로 조립한 목록 화면 (첫 페이지 컴포넌트)
- `frontend/src/App.tsx` — `<Routes><Route path="/" element={<PostListPage/>} /></Routes>`로 URL↔화면 연결 (첫 라우팅). BrowserRouter는 main.tsx에 기설치
- `frontend/src/api/posts.ts` — 첫 mock API 함수 3종: `fetchPosts(params?)`(setTimeout 지연 + q/tagId 필터를 **함수 내부**에서, 반환 `{items, nextCursor}`), `fetchPostById(id)`(`find`→없으면 `throw`→`Promise<Post>`), `createPost(input:NewPost)`(스프레드+`crypto.randomUUID()`+`toISOString()`로 완성형 Post 만들어 `MOCK_POSTS.push`). 시그니처를 REST 계약 모양으로 맞춰 다음 주 axios는 본문만 교체 가능
