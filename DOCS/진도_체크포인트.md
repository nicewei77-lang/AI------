# 진도 체크포인트 (살아있는 진행 기록)

> 에이전트는 **매 세션 시작 시 이 파일을 먼저 읽어** 어디서 이어갈지 파악하고,
> **각 단계/세션 끝에 이 파일을 갱신**한다. (커리큘럼 전체 지도는 학습코치 AGENT §5·§6, 회고는 학습_지침_개선_일지.md)

마지막 업데이트: 2026-06-07 (세션 1)

---

## 현재 위치

- **진행 중 단계:** Day 1 / 1-A → 1-B 경계
- **다음 할 일:** `tsc -b` 통과 확인 → `PostCard` 컴포넌트(첫 `.tsx`, props로 `Post` 받아 카드 렌더)
- **막판 메모:** `tsc -b` 실행은 했으나 결과 미보고. 다음 세션 첫 확인 사항.

---

## 단계별 상태

```
[x] P0   환경 세팅 (Vite/Tailwind/Router, frontend 골격) — 기세팅 확인
[~] 1-A  TS 타입: Verdict/ExcuseContext/Post/Tag/NewPost 정의
         └ 작성 완료. tsc -b 통과 확인만 남음.
[ ] 1-B  컴포넌트+props+map → PostCard/PostList 목록 렌더 (슬라이스①)
[ ] 2-A  useState+이벤트+제어폼 → 로그인폼/변명폼 (슬라이스②)
[ ] 2-B  비동기+useEffect+mock fetch+라우팅 → 목록↔상세 (슬라이스③)
[ ] 3-A  Context+ProtectedRoute+검색/태그 → 전체 흐름 (슬라이스④) [페이징=stretch]
[ ] 3-B  (stretch) Figma 목업 해석/흡수
```
(`x`=완료, `~`=진행중, ` `=미착수)

---

## 설계 결정 로그 (스펙과 달라진 점 — 다음 세션이 헷갈리지 않게)

- **2026-06-07:** `Post`에서 `situation` 제거, `title` 추가. 변명 유형 분류는 `tags`가 전담.
  (이유: `situation`과 `tags`가 둘 다 "지각/결석/마감" 분류라 역할 중복. 게시판이니 제목이 필요.)
  → 코드(`types/post.ts`), 기획서 DB 스키마·기능표, 학습코치 AGENT 드릴 예시 모두 반영 완료.

---

## 완성한 산출물

- `frontend/src/types/post.ts` — `Verdict`, `ExcuseContext`, `Post`, `Tag`, `NewPost`
