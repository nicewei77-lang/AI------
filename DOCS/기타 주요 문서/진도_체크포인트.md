# 진도 체크포인트 (resume 상태 — 히스토리 아카이브 아님)

> 매 세션 시작 시 먼저 읽어 이어갈 지점 파악, 각 세션 끝에 갱신. **다음에 쓸 정보만 남기고 히스토리는 가지친다**(세션 회고는 개념노트·`학습_지침_개선_일지.md`). 커리큘럼 = 백엔드 학습코치 AGENT §5·§6.
> **방지책(갱신 때 자가검사):** ①완료분은 **파일명만** — 함수·구현 세부 재서술 금지(코드가 1차 출처). ②"현재 위치"는 **≤4줄**, 넘으면 가지친다. ③새 항목 추가 시 **끝난 복선·stale 표기 1개 이상 삭제**(추가=삭제 쌍).

마지막 업데이트: 2026-06-17 (ProjectLens 배포 연결 준비: 프론트 env/API + 백엔드 Render Blueprint)

---

## 현재 위치

- **국면:** ProjectLens 배포 연결 준비 완료. 프론트 API base는 `VITE_API_URL`, 백엔드 CORS는 `BACKEND_CORS_ORIGINS`, 백엔드+Agent+local/private MCP는 Render Docker Blueprint로 묶었다.
- **완료 파일:** `frontend/src/api/http.ts`, `frontend/src/vite-env.d.ts`, `frontend/.env.example`, `backend/app/config.py`, `backend/app/main.py`, `backend/app/db.py`, `backend/scripts/apply_schema.py`, `backend/.env.example`, `Dockerfile.backend`, `.dockerignore`, `render.yaml`.
- **검증:** `frontend/npm run build`, `backend/.venv compileall`, 백엔드 `/health`·`/posts`, 브라우저 `http://localhost:5173` 목록 렌더/콘솔 에러 없음 확인.
- **주의:** 클라우드 실연결은 Render 서비스 생성 후 백엔드 URL을 Vercel `VITE_API_URL`에 넣고, Render `BACKEND_CORS_ORIGINS`에 Vercel URL 및 `OPENAI_API_KEY`를 설정해야 완료된다.

---

## 백엔드 단계 (진도 판정 = 동작 검증)

```text
[x] B0  환경 — uvicorn /health 200 + /docs, Docker Postgres(pgvector pg16) healthy, venv+requirements
[x] B1  설정·골격 — config.py(.env)·main.py(/health)·db.py(엔진/세션/get_db/ping). `python -m app.db`→`1` 검증
[x] B2  모델링 — ERD→schema.sql 6테이블 DB 생성, models.py 선언형 모델 6개(복합PK post_tags·복합UNIQUE votes). configure_mappers 통과 + DB \dt 6테이블 확인
[x] B3  CRUD+REST+투표 — 스키마/라우터/리포, 검색·태그·페이징·정렬·투표(토글 트랜잭션·1인1표)
[x] B4  인증+연동 — 해싱(bcrypt)·JWT·get_current_user(+optional) 가드·CORS·rate limit(slowapi)·myVote. 401/토큰/myVote end-to-end 검증. (프론트 axios 연결만 8·9로)
[x] M0  ProjectLens 데이터 계층 — posts 클린 전환 + ai_reports/mcp_evidences/embeddings + pgvector + OpenAI 기본 설정. DB 적용·API smoke 검증.
[x] M1  local/private MCP 사이트 분석 도구 — `mcp-server/`, `backend/app/mcp_client/`, 공개 URL fetch/status + SSRF 가드 검증.
[x] M2  Agent 분석 파이프라인 — `backend/app/ai/`, `services/analysis_service.py`, `routers/analysis.py`, mock runner + 실제 MCP로 ai_reports/mcp_evidences/posts 상태 갱신 검증. 실 OpenAI `gpt-5.5` eval completed 검증.
[x] M3  리포트 카드 UI — 분석 타입/API + 구조화 카드 + ProjectForm + 상세 실행 버튼/최신 리포트 로드. 브라우저 smoke 통과.
[x] M4  RAG + Agent Function Calling 보강 — embeddings 저장/pgvector cosine 검색 + SimilarProjectsCard + function_tool 래퍼 + GitHub README MCP. 실 eval에서 MCP evidence/RAG evidence 저장 검증.
[x] M5  데이터 수집 개방 + 시드 — 본인 프로젝트 시드 5개 + 외부 사용자 smoke + 깨진 URL smoke + 업로드 고지. mock/fake 경로 검증 완료.
[x] Q1  실패 모드 견고화 — URL 실패/SSRF/본문 빈약+README 폴백/RAG 빈 결과/refusal·failed·need_more_info/running 상태 smoke 통과.
[x] Q2  프롬프트/스키마 튜닝 — 사이트 구조/서비스 본질/핵심 인사이트 필드 + 확인/추정 분리. 3개 고정 URL 실 eval Q2 루브릭 통과.
[x] Q3  포트폴리오/발표 카드 — 기존 리포트 재사용 카드 + 복사 버튼. 브라우저 `/posts/99`에서 표시/복사됨 라벨 확인.
[x] Q4  RAG 가중 정렬 — 공개 seed 10개 + post embedding 34개. weighted 자동 적용, cosine 대비 태그/유형/최신성 재정렬 비교 완료.
[x] Q5  MCP 확장 검토 — Q1~Q5 시점에는 보류했으나 Q6~Q11에서 bounded 확장으로 승격.
[x] Q6  MCP evidence 계약 확장 — `site_context`/`rendered_site`/`screenshot`/`lighthouse` schema·타입·카드·요약 반영.
[x] Q7  bounded site context — same-origin depth-1, 최대 5페이지, SSRF/redirect 재검증.
[x] Q8  screenshot evidence — Playwright 화면 캡처 metadata/visible text만 저장, raw image/base64 미저장.
[x] Q9  Lighthouse summary — local CLI 점수/key audits 요약 저장, raw report 미저장.
[x] Q10 통합 검증 — 직접 tool check + SSRF + optional failure + evidence/latest report smoke 통과.
[x] Q11 async analysis polling — `POST /analysis/jobs`, `GET /analysis/status`, 프론트 polling. 별도 `analysis_jobs` 테이블은 아직 없음.
[x] Q12 차단 URL 보강 — Playwright rendered fallback + `blocked_by_site`/`need_more_info` 분류. stealth/CAPTCHA/proxy/session 우회는 제외.
[x] PV2 리포트 V2 피봇 — V2 스키마/프롬프트/리포트 UI/한계 UX. compileall/build/Q1-Q5/MCP expansion/real quality eval 통과.
[x] PV2.1 근거 추적성 — `evidence.findings`, ID 기반 `evidence_refs`, `analysis_confidence`, `portfolio_translation.source_finding_ids`. real eval 저장 JSON에서 필드 확인.
```

**존재 파일(학습자 직접):** config.py · main.py · db.py · db/schema.sql(ProjectLens 데이터 계층) · db/ALIBAI.drawio+svg(구 ERD). **인프라(코치):** docker-compose.yml · requirements.txt(+OpenAI/pgvector/httpx) · .env · mcp-server/ · backend/app/mcp_client/ · backend/app/ai/.

---

## 설계 결정 로그 (스펙과 달라진 계약 — 백엔드가 지금 씀)

- **투표(백엔드 필수):** 점수=net(up−down), 요청 `{value:1|-1}`, 응답 `{score,myVote}`. 같은 값 재클릭=취소, 반대 값=전환. `POST /posts/{id}/vote`, `votes` 테이블. 댓글은 평면.
- **JSON 계약(camel↔snake):** 외부 camelCase ↔ DB snake_case를 Pydantic alias로(`serviceUrl↔service_url`, `oneLiner↔one_liner`, `createdAt↔created_at`). 본문은 `body`. 목록 태그 쿼리는 `?tag=`, 유형 필터는 `?postType=`.
- **태그 ID:** 외부 `Tag.id`=문자열 slug(`t1`), DB FK=숫자 `tags.id`, `tags.slug UNIQUE`로 연결. 생성 스키마 내부 `tag_ids`/외부 alias `tagIds`/값 `string[]` slug.
- **Post.score** 백엔드 응답 포함. **score=저장 컬럼 유지(A안 확정, 2026-06-13).** 투표 토글 트랜잭션 안에서 score를 ±갱신(votes 변경과 한 트랜잭션). JOIN은 태그필터·작성자에서 학습.
- **AI 분석 상태:** `ai_reports.status`는 `completed|need_more_info|failed|refused`, `posts.analysis_status`는 기존 CHECK 제약상 `refused` 미포함이라 `refused -> failed`로 매핑. 최신 리포트 없음은 `GET /posts/{id}/analysis/latest` 404.
- **RAG·도구:** post embedding 20개 이상이면 weighted(`semantic*0.65 + tag_overlap*0.15 + vote*0.10 + recency*0.05 + same_type*0.05`) 자동 전환. MCP allowlist는 `check_deploy_status`, `fetch_site_overview`, `fetch_github_readme`, `fetch_site_context`, `fetch_rendered_site_overview`, `capture_screenshot`, `run_lighthouse_summary`.
