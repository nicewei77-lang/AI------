# 진도 체크포인트 (resume 상태 — 히스토리 아카이브 아님)

> 매 세션 시작 시 먼저 읽어 이어갈 지점 파악, 각 세션 끝에 갱신. **다음에 쓸 정보만 남기고 히스토리는 가지친다**(세션 회고는 개념노트·`학습_지침_개선_일지.md`). 커리큘럼 = 백엔드 학습코치 AGENT §5·§6.
> **방지책(갱신 때 자가검사):** ①완료분은 **파일명만** — 함수·구현 세부 재서술 금지(코드가 1차 출처). ②"현재 위치"는 **≤4줄**, 넘으면 가지친다. ③새 항목 추가 시 **끝난 복선·stale 표기 1개 이상 삭제**(추가=삭제 쌍).

마지막 업데이트: 2026-06-15 (ProjectLens M4 RAG + Agent Function Calling 보강 완료)

---

## 현재 위치

- **국면:** ProjectLens M4 완료. 제품 우선·탑다운 모드 유지.
- **현 코드 상태:** `backend/app/rag/`, `backend/app/ai/tools.py`, `backend/app/ai/runner.py`, `services/analysis_service.py`, `mcp-server/tools/github.py`, `SimilarProjectsCard.tsx`.
- **검증:** compile/build 통과, fake embedding 시드 5개 + pgvector top-k + empty path + MCP wrapper 3종 evidence 저장 확인. `OPENAI_API_KEY` 없음 → 실제 gpt-5.5 Function Calling은 미검증.
- **다음 할 일:** M5 데이터 수집 개방 + 시드 — 본인 시드 5개 정리 후 업로드 동의 문구와 외부 사용자 업로드 smoke.

---

## 백엔드 단계 (진도 판정 = 동작 검증)

```text
[x] B0  환경 — uvicorn /health 200 + /docs, Docker Postgres(pgvector pg16) healthy, venv+requirements
[x] B1  설정·골격 — config.py(.env)·main.py(/health)·db.py(엔진/세션/get_db/ping). `python -m app.db`→`1` 검증
[x] B2  모델링 — ERD→schema.sql 6테이블 DB 생성, models.py 선언형 모델 6개(복합PK post_tags·복합UNIQUE votes). configure_mappers 통과 + DB \dt 6테이블 확인
[x] B3  CRUD+REST+투표 — 스키마/라우터/리포, 검색·태그·페이징·정렬·투표(토글 트랜잭션·1인1표)
[x] B4  인증+연동 — 해싱(bcrypt)·JWT·get_current_user(+optional) 가드·CORS·rate limit(slowapi)·myVote. 401/토큰/myVote end-to-end 검증. (프론트 axios 연결만 8·9로)
[x] M0  ProjectLens 데이터 계층 — posts 클린 전환 + ai_reports/mcp_evidences/embeddings + pgvector + OpenAI 기본 설정. DB 적용·API smoke 검증.
[x] M1  local/private MCP 사이트 분석 도구 — `mcp-server/`, `backend/app/mcp_client/`, 공개 URL fetch/status + SSRF 가드 검증.
[x] M2  Agent 분석 파이프라인 — `backend/app/ai/`, `services/analysis_service.py`, `routers/analysis.py`, mock runner + 실제 MCP로 ai_reports/mcp_evidences/posts 상태 갱신 검증. `OPENAI_API_KEY` 없음 → 실제 gpt-5.5 호출은 미검증.
[x] M3  리포트 카드 UI — 분석 타입/API + 구조화 카드 + ProjectForm + 상세 실행 버튼/최신 리포트 로드. 브라우저 smoke 통과.
[x] M4  RAG + Agent Function Calling 보강 — embeddings 저장/pgvector cosine 검색 + SimilarProjectsCard + function_tool 래퍼 + GitHub README MCP. `OPENAI_API_KEY` 없음 → fake embedding/tool wrapper smoke, 실제 Agent Function Calling 미검증.
```

**존재 파일(학습자 직접):** config.py · main.py · db.py · db/schema.sql(ProjectLens 데이터 계층) · db/ALIBAI.drawio+svg(구 ERD). **인프라(코치):** docker-compose.yml · requirements.txt(+OpenAI/pgvector/httpx) · .env · mcp-server/ · backend/app/mcp_client/ · backend/app/ai/.

---

## 설계 결정 로그 (스펙과 달라진 계약 — 백엔드가 지금 씀)

- **투표(백엔드 필수):** 점수=net(up−down), 요청 `{value:1|-1}`, 응답 `{score,myVote}`. 같은 값 재클릭=취소, 반대 값=전환. `POST /posts/{id}/vote`, `votes` 테이블. 댓글은 평면.
- **JSON 계약(camel↔snake):** 외부 camelCase ↔ DB snake_case를 Pydantic alias로(`serviceUrl↔service_url`, `oneLiner↔one_liner`, `createdAt↔created_at`). 본문은 `body`. 목록 태그 쿼리는 `?tag=`, 유형 필터는 `?postType=`.
- **태그 ID:** 외부 `Tag.id`=문자열 slug(`t1`), DB FK=숫자 `tags.id`, `tags.slug UNIQUE`로 연결. 생성 스키마 내부 `tag_ids`/외부 alias `tagIds`/값 `string[]` slug.
- **Post.score** 백엔드 응답 포함. **score=저장 컬럼 유지(A안 확정, 2026-06-13).** 투표 토글 트랜잭션 안에서 score를 ±갱신(votes 변경과 한 트랜잭션). JOIN은 태그필터·작성자에서 학습.
- **AI 분석 상태:** `ai_reports.status`는 `completed|need_more_info|failed|refused`, `posts.analysis_status`는 기존 CHECK 제약상 `refused` 미포함이라 `refused -> failed`로 매핑. 최신 리포트 없음은 `GET /posts/{id}/analysis/latest` 404.
- **M4 RAG/도구:** 초기 유사도는 cosine 단독, 임계값 미만·데이터 부족은 빈 상태. `mcp_evidences.report_id`는 `ai_reports` 생성 후 붙이고, GitHub README는 owner/repo 파싱→`api.github.com` 고정 호출.
