# 진도 체크포인트 (resume 상태 — 히스토리 아카이브 아님)

> 매 세션 시작 시 먼저 읽어 이어갈 지점 파악, 각 세션 끝에 갱신. **다음에 쓸 정보만 남기고 히스토리는 가지친다**(세션 회고는 개념노트·`학습_지침_개선_일지.md`). 커리큘럼 = 백엔드 학습코치 AGENT §5·§6.
> **방지책(갱신 때 자가검사):** ①완료분은 **파일명만** — 함수·구현 세부 재서술 금지(코드가 1차 출처). ②"현재 위치"는 **≤4줄**, 넘으면 가지친다. ③새 항목 추가 시 **끝난 복선·stale 표기 1개 이상 삭제**(추가=삭제 쌍).

마지막 업데이트: 2026-06-15 (ProjectLens 전환 문서 정렬 — OpenAI Agents SDK/gpt-5.5, local/private MCP, Structured Outputs, M0~M3 우선순위)

---

## 현재 위치

- **국면:** ALIBAI → ProjectLens 전환 문서 정렬 완료. 제품 우선·탑다운 모드, 드릴 폐기.
- **현 코드 상태:** 백엔드 B0~B4 기반은 동작 검증됨. DB/모델/프론트 타입은 아직 ALIBAI 필드(`excuse_text`, `verdict`, `credibility`, `context`)가 남아 있음.
- **다음 할 일:** `DOCS/ProjectLens_개발_계획.md`의 M0부터 시작 — posts 클린 마이그레이션 + `ai_reports`/`mcp_evidences`/`embeddings` + OpenAI 설정.
- **OpenAI/MCP 계약:** 기본 `gpt-5.5`, `reasoning.effort=medium`, Pydantic Structured Outputs, local/private MCP, prompt injection 방어, response/trace/usage/error 저장.

---

## 백엔드 단계 (진도 판정 = 동작 검증)

```text
[x] B0  환경 — uvicorn /health 200 + /docs, Docker Postgres(pgvector pg16) healthy, venv+requirements
[x] B1  설정·골격 — config.py(.env)·main.py(/health)·db.py(엔진/세션/get_db/ping). `python -m app.db`→`1` 검증
[x] B2  모델링 — ERD→schema.sql 6테이블 DB 생성, models.py 선언형 모델 6개(복합PK post_tags·복합UNIQUE votes). configure_mappers 통과 + DB \dt 6테이블 확인
[x] B3  CRUD+REST+투표 — 스키마/라우터/리포, 검색·태그·페이징·정렬·투표(토글 트랜잭션·1인1표)
[x] B4  인증+연동 — 해싱(bcrypt)·JWT·get_current_user(+optional) 가드·CORS·rate limit(slowapi)·myVote. 401/토큰/myVote end-to-end 검증. (프론트 axios 연결만 8·9로)
```

**존재 파일(학습자 직접):** config.py · main.py · db.py · db/schema.sql(6테이블) · db/ALIBAI.drawio+svg(ERD). **인프라(코치):** docker-compose.yml · requirements.txt(+greenlet=SQLAlchemy async 의존) · .env.

---

## 설계 결정 로그 (스펙과 달라진 계약 — 백엔드가 지금 씀)

- **투표(백엔드 필수):** 점수=net(up−down), 요청 `{value:1|-1}`, 응답 `{score,myVote}`. 같은 값 재클릭=취소, 반대 값=전환. `POST /posts/{id}/vote`, `votes` 테이블. 댓글은 평면.
- **JSON 계약(camel↔snake):** 외부 camelCase ↔ DB snake_case를 Pydantic alias로(`excuseText↔excuse_text`, `createdAt↔created_at`). 목록 태그 쿼리는 `?tag=`.
- **태그 ID:** 외부 `Tag.id`=문자열 slug(`t1`), DB FK=숫자 `tags.id`, `tags.slug UNIQUE`로 연결. 생성 스키마 내부 `tag_ids`/외부 alias `tagIds`/값 `string[]` slug.
- **Post.score** 백엔드 응답 포함. **score=저장 컬럼 유지(A안 확정, 2026-06-13).** 상세문서는 매번 SUM 계산(B안)이라 적었으나, schema.sql·models.py에 `posts.score` 컬럼이 이미 있고 `PostOut.from_attributes`가 `post.score`를 그대로 읽는 구조라 (A)로 감 → 투표 토글 트랜잭션 안에서 score를 ±갱신(votes 변경과 한 트랜잭션). JOIN은 태그필터·작성자에서 학습. **임베딩 적재는 AI 단계로 분리**(B는 DB 저장만).
- (프론트 델타) `Post`: `situation` 제거·`title` 추가(분류는 tags 전담). `fetchPosts` 태그 파라미터명 `tagId`.
